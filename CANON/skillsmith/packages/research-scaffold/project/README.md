# project

Place the actual project work surface here.

This directory is the product work surface for the downstream project. It may contain application code, local app assets, site content, game logic, models, pipelines, datasets, prompts, or other project artifacts. If a change claims to improve the project itself, it should normally happen here.

Keep control-plane records, plans, scores, and iteration history in the scaffold root instead of mixing them into this directory.
