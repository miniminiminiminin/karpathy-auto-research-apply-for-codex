# API Change Note

```text
SEAM := {
  endpoint_or_contract,
  old_behavior,
  new_behavior
}

IMPACT := {
  consumers,
  compatibility_risk,
  rollout_or_rollback_note,
  actionable_error_change,
  performance_impact
}

PROOF := {
  verification,
  docs_updated
}
```
