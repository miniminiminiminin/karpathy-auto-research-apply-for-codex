# Producer Blast Radius Planning

```text
PASS IF
  direct_consumers_are_identified
  AND consumer_criticality_is_classified
  AND stakeholder_or_owner_is_named
  AND migration_sequencing_need_is_decided
  AND breakage_risk_from_timing_schema_or_semantics_is_recorded

FAIL IF
  consumer_impact_is_assumed
  OR verification_focus_for_risky_consumers_is_missing
```
