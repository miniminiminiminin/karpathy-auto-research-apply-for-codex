# Loop Convergence Rules

- choose the smallest slice that can materially improve purpose performance
- do not repeat an iteration shape three times without recording what changed in the hypothesis
- if score stays flat and complexity rises, challenge the slice choice
- if regressions accumulate, route to review or architecture instead of forcing more execution
- stop iterating when the app is ready enough to consider release
